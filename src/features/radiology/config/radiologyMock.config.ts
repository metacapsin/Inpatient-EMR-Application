/** Set to `false` when Radiology backend routes are live — components stay unchanged. */
export const USE_RADIOLOGY_MOCK = true;

/** Max wait for real API before falling back to in-memory mock data. */
export const RADIOLOGY_API_TIMEOUT_MS = 5_000;
